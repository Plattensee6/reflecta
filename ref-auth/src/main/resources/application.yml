

spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/reflecta
    username: reflecta
    password: reflecta
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true